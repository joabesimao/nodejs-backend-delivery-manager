/* import { RegisterModel } from "../../../../domain/models/register/register-model";

export const map = (register:any) :RegisterModel => {
  const {_id,...registerWithoutId} = register
  return Object.assign({},registerWithoutId,{id:_id})

} 
 */
